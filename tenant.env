# Local dev env for the cloned tenant app (the repo's .env is gitignored, so it
# is not in the clone). The CLI injects this into the sandbox as the app's .env
# (and as a Modal Secret) when INJECT_ENV_FILE points here.
APP_GREETING=Hello from the POC env
TENANT_NAME=acme-demo
